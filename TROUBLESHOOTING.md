"""
TROUBLESHOOTING: Решение проблем при интеграции CDEK

Если что-то не работает - смотри здесь!
"""

# ════════════════════════════════════════════════════════════════════════════
# ❌ ПРОБЛЕМА: "CDEK_CLIENT_ID или CDEK_CLIENT_SECRET не установлены"
# ════════════════════════════════════════════════════════════════════════════

"""
🔴 ЧТО ВИДИШЬ:
    ⚠️ CDEK_CLIENT_ID или CDEK_CLIENT_SECRET не установлены!
    Доставка будет рассчитываться как 500 ₽ по умолчанию

✅ РЕШЕНИЕ (Приоритет):

    1️⃣ Проверь .env файл существует:
       ls -la .env
       
    2️⃣ Убедись что ключи там:
       cat .env | grep CDEK
       
       Должны быть две строки:
       CDEK_CLIENT_ID=4I5vLAbLUPdMIOEhVD0osn4fS0fvTttj
       CDEK_CLIENT_SECRET=g1WXBI56G3ZAPrY0TleKblVIwsnMCm8J
       
    3️⃣ Если пусто - заполни ключи:
       # Открой .env и добавь:
       CDEK_CLIENT_ID=твой_клиент_id
       CDEK_CLIENT_SECRET=твой_клиент_secret
       
    4️⃣ Перезагрузи бота:
       # Заверши текущий процесс (Ctrl+C)
       # И запусти снова:
       python bot_with_cdek.py
       
    5️⃣ Проверь что ключи правильные:
       python cdek_integration.py
       # Должен вывести ✅ Токен получен

💡 ОТКУДА ВЗЯТЬ КЛЮЧИ:
    Зайди на https://partner.cdek.ru
    Авторизуйся
    Раздел "API ключи" или "Интеграция"
    Скопируй CLIENT_ID и CLIENT_SECRET
"""


# ════════════════════════════════════════════════════════════════════════════
# ❌ ПРОБЛЕМА: "Connection refused" при запуске FastAPI
# ════════════════════════════════════════════════════════════════════════════

"""
🔴 ЧТО ВИДИШЬ:
    ERROR: Server startup failed
    Failed to bind to address 127.0.0.1:8000
    Address already in use

✅ РЕШЕНИЕ:

    1️⃣ Порт 8000 занят другим процессом
    
       Найди какой процесс занимает порт:
       
       # На Linux/Mac:
       lsof -i :8000
       
       # На Windows:
       netstat -ano | findstr 8000
       
    2️⃣ Убей процесс:
       
       # Linux/Mac:
       kill -9 <PID>
       
       # Windows:
       taskkill /PID <PID> /F
       
    3️⃣ Или используй другой порт:
       
       Отредактируй bot_with_cdek.py:
       
       uvicorn_config = uvicorn.Config(
           app,
           host="127.0.0.1",
           port=8001,  # Измени на 8001
           ...
       )
       
    4️⃣ Запусти снова:
       python bot_with_cdek.py

💡 КАК ПРЕДОТВРАТИТЬ:
    Всегда корректно завершай бот (Ctrl+C)
    Не запускай несколько экземпляров на одном порту
"""


# ════════════════════════════════════════════════════════════════════════════
# ❌ ПРОБЛЕМА: "Timeout при расчете доставки"
# ════════════════════════════════════════════════════════════════════════════

"""
🔴 ЧТО ВИДИШЬ:
    ⏱️ Timeout при расчете доставки
    
    Или в Mini App сообщение:
    "⚠️ Используется стандартная стоимость"

✅ РЕШЕНИЕ:

    1️⃣ Проверь интернет:
       ping google.com
       ping api.cdek.ru
       
    2️⃣ Проверь что CDEK API работает:
       curl https://api.cdek.ru/v2/location/cities
       
       Если ошибка - CDEK может быть недоступен
       
    3️⃣ Это нормально! Код обрабатывает это:
       
       Когда timeout происходит:
       ✅ Возвращается стоимость 500 ₽ (по умолчанию)
       ✅ Заказ НЕ сломается
       ✅ Пользователь подходит за персональным расчетом
       
    4️⃣ Если timeout часто:
       
       Увели timeout в cdek_integration.py:
       
       async with session.post(..., timeout=aiohttp.ClientTimeout(total=30)) as resp:
                                                                            ↑
                                                                    было 15, теперь 30

💡 ЭТО НЕ ОШИБКА!
    Timeout = CDEK медленный сегодня
    Код правильно это обрабатывает
    Пользователь все равно может заказать
"""


# ════════════════════════════════════════════════════════════════════════════
# ❌ ПРОБЛЕМА: "Город не найден" при вводе
# ════════════════════════════════════════════════════════════════════════════

"""
🔴 ЧТО ВИДИШЬ:
    В Mini App подсказок города нет
    
    Или при выборе города:
    "⚠️ Используется стандартная стоимость" (город не найден)

✅ РЕШЕНИЕ:

    1️⃣ Проверь написание города:
       ✅ "Москва" (с большой буквы и русские буквы)
       ❌ "москва" (не сработает)
       ❌ "Moscow" (англий, не сработает)
       
    2️⃣ Используй города из списка в index_updated.html:
       
       Доступные города (первые 30):
       Москва, Санкт-Петербург, Новосибирск, Екатеринбург,
       Казань, Челябинск, Омск, Самара, Уфа, Ростов-на-Дону,
       ...
       
       Полный список: popularCities в index_updated.html
       
    3️⃣ Если города нет в списке - добавь его:
       
       index_updated.html, строка ~200:
       
       const popularCities = [
           "Москва",
           "Твой-город",  // Добавь сюда
           ...
       ];
       
    4️⃣ Проверь что город есть в CDEK:
       
       python cdek_integration.py
       
       2️⃣ ТЕСТ ПОИСКА ГОРОДА:
       Если твой город там не появляется → его нет в CDEK
       
    5️⃣ Проверь логи:
       
       Ищи в логах:
       ✅ Найден код города 'Москва': 1
       ⚠️ Город 'Неизвестный' не найден

💡 ЧТО ДЕЛАТЬ ЕСЛИ ГОРОДА НЕТ В CDEK:
    1. Уточни точное название в справочнике CDEK
    2. Или используй ближайший областной центр
    3. Жди пока CDEK добавит этот город в API
"""


# ════════════════════════════════════════════════════════════════════════════
# ❌ ПРОБЛЕМА: Mini App не открывается в боте
# ════════════════════════════════════════════════════════════════════════════

"""
🔴 ЧТО ВИДИШЬ:
    Кнопка "🛍️ Открыть магазин" не появляется
    
    Или нажимаешь - ничего не открывается
    
    Или черный экран

✅ РЕШЕНИЕ:

    1️⃣ Проверь что MINI_APP_URL правильный:
       
       .env файл:
       MINI_APP_URL=https://yourdomain.com/index_updated.html
       
       ✅ Должен быть полный URL (https://...)
       ❌ Не может быть локальный файл (file://...)
       
    2️⃣ Проверь что URL доступен:
       
       Открой в браузере:
       https://yourdomain.com/index_updated.html
       
       Должна загрузиться страница магазина
       
    3️⃣ Если не загружается - проверь:
       
       - Хостинг работает?
       - Файл есть на сервере?
       - Права доступа правильные?
       - SSL сертификат валидный?
       
    4️⃣ Если Mini App черный экран:
       
       Открой консоль браузера (F12):
       Console tab
       
       Ищи ошибки. Обычно:
       - CORS ошибка (нужно настроить в FastAPI)
       - Проблема с JS (ошибка синтаксиса)
       - Fetch не может достигнуть API

💡 CORS ОШИБКА:
    Если видишь в консоли:
    "CORS policy: Cross-Origin Request Blocked"
    
    Добавь в bot_with_cdek.py:
    
    from fastapi.middleware.cors import CORSMiddleware
    
    app.add_middleware(
        CORSMiddleware,
        allow_origins=["*"],
        allow_credentials=True,
        allow_methods=["*"],
        allow_headers=["*"],
    )
"""


# ════════════════════════════════════════════════════════════════════════════
# ❌ ПРОБЛЕМА: "Fetch failed" при расчете доставки в Mini App
# ════════════════════════════════════════════════════════════════════════════

"""
🔴 ЧТО ВИДИШЬ:
    В консоли браузера (F12):
    Fetch failed: ...
    
    Или в Mini App:
    Иконка загрузки "спиннер" крутится бесконечно

✅ РЕШЕНИЕ:

    1️⃣ Проверь что бот работает:
       
       В другом терминале выполни:
       curl http://127.0.0.1:8000/api/health
       
       Должен вернуться JSON с "status": "ok"
       
       Если ошибка - бот не запущен:
       python bot_with_cdek.py
       
    2️⃣ Проверь что Mini App подключается к правильному API:
       
       index_updated.html, функция selectCity():
       
       const response = await fetch('/api/calculate-shipping', {
           method: 'POST',
           ...
       });
       
       Путь '/api/calculate-shipping' - это ОТНОСИТЕЛЬНЫЙ путь
       
       Он подключится к тому же домену, где загружен HTML:
       
       ✅ Если Mini App на https://example.com/index.html
          → API должан быть на https://example.com/api/calculate-shipping
          
       ❌ Не может быть на другом домене без CORS
       
    3️⃣ На локальной машине:
       
       Если Mini App на одной машине, API на другой:
       
       Отредактируй index_updated.html:
       
       // const response = await fetch('/api/calculate-shipping', {
       const response = await fetch('http://192.168.1.100:8000/api/calculate-shipping', {
       
    4️⃣ Проверь сетевые ошибки (F12 → Network tab):
       
       Нажми в Mini App на город
       
       Посмотри Network tab
       
       Ищи запрос к /api/calculate-shipping
       
       Если красный статус код → проблема на сервере
       Status 404 → endpoint не существует
       Status 500 → ошибка в коде

💡 БЫСТРЫЙ DEBUG:
    1. Открой консоль одновременно в 3 местах:
       - Терминал где запущен бот
       - Браузер на Mini App (F12)
       - curl для тестирования
    
    2. Нажми действие в Mini App
    3. Смотри где появилось сообщение об ошибке
"""


# ════════════════════════════════════════════════════════════════════════════
# ❌ ПРОБЛЕМА: Номер телефона не форматируется
# ════════════════════════════════════════════════════════════════════════════

"""
🔴 ЧТО ВИДИШЬ:
    Вводишь в поле телефона цифры
    
    Они не форматируются как "+7 999 123 45 67"

✅ РЕШЕНИЕ:

    1️⃣ Это должно работать в index_updated.html
       
       Проверь что функция formatPhone подключена:
       
       document.getElementById('phone').addEventListener('input', formatPhone);
       
    2️⃣ Если не работает - проверь консоль (F12):
       
       Может быть ошибка JS
       
    3️⃣ Альтернатива - используй простое поле:
       
       index_updated.html:
       
       <!-- Было: type="tel" -->
       <input type="input" id="phone" ...>
       
       Форматирование необязательно
       
    4️⃣ На продакшене форматирование не критично:
       
       Главное что номер содержит цифры
       Форматирование - только для удобства пользователя

💡 ТЕЛЕФОН РАБОТАЕТ ЕСЛИ:
    Пользователь может вводить цифры
    При нажатии отправки - номер валидируется
    Если валидный → заказ отправляется
"""


# ════════════════════════════════════════════════════════════════════════════
# ❌ ПРОБЛЕМА: "ModuleNotFoundError: No module named 'aiogram'"
# ════════════════════════════════════════════════════════════════════════════

"""
🔴 ЧТО ВИДИШЬ:
    Traceback ...
    ModuleNotFoundError: No module named 'aiogram'

✅ РЕШЕНИЕ:

    Не установлены зависимости!
    
    1️⃣ Установи требуемые пакеты:
       pip install -r requirements.txt
       
    2️⃣ Если не работает - установи вручную:
       pip install aiogram==3.5.0
       pip install aiohttp==3.9.3
       pip install fastapi==0.104.1
       pip install uvicorn==0.24.0
       pip install python-dotenv==1.0.0
       
    3️⃣ Проверь что все установилось:
       pip list | grep aiogram
       
       Должна быть версия 3.x
       
    4️⃣ Если все еще не работает:
       
       # Пересоздай окружение (если используешь venv)
       python -m venv venv
       source venv/bin/activate  # Linux/Mac
       # или
       venv\\Scripts\\activate  # Windows
       
       pip install -r requirements.txt

💡 ОБЩИЙ СОВЕТ:
    Всегда работай в виртуальном окружении (venv)
    Так разные проекты не конфликтуют
"""


# ════════════════════════════════════════════════════════════════════════════
# ❌ ПРОБЛЕМА: Заказ отправляется но данных нет
# ════════════════════════════════════════════════════════════════════════════

"""
🔴 ЧТО ВИДИШЬ:
    Заказ отправлен (пользователь видит подтверждение)
    
    Но в боте нет сообщения
    
    Или данные неполные

✅ РЕШЕНИЕ:

    1️⃣ Проверь что bot.send_message вообще вызывается:
       
       bot_with_cdek.py, функция handle_web_app_order():
       
       Добавь логирование:
       
       @router.message(F.web_app_data)
       async def handle_web_app_order(message: types.Message):
           print("📦 GOT ORDER!")  # Добавь это
           print(message.web_app_data.data)  # И это
           ...
       
    2️⃣ Перезагрузи бота и отправь заказ снова
       
       Посмотри терминал где запущен бот
       
       Должны быть логи:
       📦 GOT ORDER!
       {"item": "zip-hoodie", ...}
       
    3️⃣ Если логи не появляются:
       
       Handler не вызывается
       
       Может быть router не зарегистрирован:
       
       В aiogram нужно:
       dp.include_router(router)
       
    4️⃣ Если данные неполные:
       
       Может быть HTML форма не заполняет все поля
       
       Проверь index_updated.html, функция sendOrder():
       
       const orderData = {
           item: p.title,
           price: p.price,
           shipping_cost: currentShippingCost,
           shipping_city: city,
           size: selectedSize,
           customer: fio,
           phone: phone,
           email: email,
           address: address,
           index: post_index,
           tg_user: tg_c
       };

💡 ДЕБАГ СООБЩЕНИЕ:
    Добавь в терминал бота:
    
    import json
    print(json.dumps(order, indent=2, ensure_ascii=False))
    
    Увидишь точно какие данные приходят
"""


# ════════════════════════════════════════════════════════════════════════════
# ❌ ПРОБЛЕМА: "SyntaxError" при запуске
# ════════════════════════════════════════════════════════════════════════════

"""
🔴 ЧТО ВИДИШЬ:
    SyntaxError: invalid syntax
    File "...", line 42
      ↑ ошибка на строке 42

✅ РЕШЕНИЕ:

    1️⃣ Открой файл и найди строку 42:
       
       Посмотри на код
       
       Обычные ошибки:
       - Не закрыта скобка ( или {
       - Не закрыта кавычка " или '
       - Забыт двоеточие : после def
       - Ошибка в отступе (indentation)
       
    2️⃣ Проверь что скопировал код правильно:
       
       Может быть при копировании забыл символ
       
    3️⃣ Используй Python linter для проверки:
       
       # На Linux/Mac
       python -m py_compile cdek_integration.py
       
       # Если ошибка - выведет точно где
       
    4️⃣ Распространенные ошибки:
       
       ❌ def my_function         (нет : в конце)
       ✅ def my_function():
       
       ❌ if x = 5:              (одинарный = вместо ==)
       ✅ if x == 5:
       
       ❌ return x               (4 пробела - но это не видно)
       ✅    return x             (правильный отступ)

💡 КОПИРОВАНИЕ КОДА:
    - Убедись что скопировал весь файл
    - Проверь что нет строк типа "..."
    - Используй IDE с syntax highlighting
"""


# ════════════════════════════════════════════════════════════════════════════
# 📞 ОБЩИЕ СТРАТЕГИИ DEBUG
# ════════════════════════════════════════════════════════════════════════════

"""
🔍 МЕТОДИКА ПОИСКА ОШИБКИ:

    1. Прочитай сообщение об ошибке (не пропусти!)
    2. Найди строку кода в сообщении
    3. Посмотри что там написано
    4. Добавь print() или logger.info() строчкой ниже
    5. Запусти снова и посмотри вывод
    6. Смотри логи от начала запуска
    7. Сравни с рабочим примером

📋 ПРОВЕРОЧНЫЙ ШАБЛОН:

    ☐ Все файлы на месте (cdek_integration.py, bot_with_cdek.py, etc)
    ☐ .env файл существует и заполнен
    ☐ Зависимости установлены (pip list)
    ☐ Бот запустился без ошибок
    ☐ API слушает на 127.0.0.1:8000
    ☐ CDEK токен получен (в логах ✅)
    ☐ Mini App открывается
    ☐ Город выбирается с подсказками
    ☐ Доставка рассчитывается
    ☐ Заказ отправляется
    ☐ Админ видит уведомление
    ☐ Покупатель видит подтверждение

🎯 ПРИОРИТЕТ БАГ ФИКСОВ:

    1️⃣ КРИТИЧНЫЕ (заказ не отправляется)
    2️⃣ ВЫСОКИЙ (доставка не рассчитывается)
    3️⃣ СРЕДНИЙ (UI выглядит странно)
    4️⃣ НИЗКИЙ (косметические проблемы)
"""


if __name__ == "__main__":
    import sys
    print(__doc__)
    print("\n" + "="*80)
    print("✅ Надеюсь это решило твою проблему!")
    print("💡 Если нет - добавь print() в код и ищи где оно не выводится")
    print("="*80)
